<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Direct all requests to index.html (except real files/folders)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>